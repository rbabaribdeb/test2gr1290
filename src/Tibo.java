/**
 * A simple Java class template.
 * This class is a placeholder and contains comments
 * to guide the addition of attributes, constructors, and methods.
 */
public class Tibo {

    // ici mettre les attributs (here to put the attributes)

    // --- ici mettre les constructeurs (here to put the constructors) ---

    // --- ici mettre les methodes (here to put the methods) ---

    // ici mettre les getters (here to put the getters)

    // ici mettre les setters (here to put the setters)

    // --- ici mettre la methode toString (here to put the toString method) ---

}